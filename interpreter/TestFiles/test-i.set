program main

	Nat : n;
	Int : x;
	Real : y;
	# Text : z;
	# Bool : b;
	# Univ : u;
	# Pointer : p;

	# x := -1;
	# y := -1;
	n := (1 - 2) + 2 + (5 - 8);
	n := ((1 - 2) + 2) + (5 - 8);
	n := ((1 - 2) + 2 + 5) - 8;
	n := (((1 - 2) + 2) + (5 - 8));
	n := (2 * 3) + 2;
	n := 2 * (3 + 2);
	n := 2 + 3 * 2;
	n := (2 + 3) * 2;

	# x := 1 + 4;
	# y := 0.5 * 2.0;
	# z  := "Ohayo";
	# b := True;
end main


# proc show (Nat number)
#  	Nat : n;
#  	n := number + 2;
# endproc

# func soma (Nat number1, Nat number2)
#  	Nat : sum;
#  	sum := number1 + number2;
#	return sum;
# endfunc
